In `@server/cmd/api/movies_scanner_helpers_test.go`:
- Around line 150-199: Add a subtest that exercises the cache-hit branch by
creating a persistent movieScanContext and calling app.getOrCreateArtist twice
with the same TMDB ID but different metadata, e.g., first call with empty or
minimal profilePath and second with a non-empty profilePath; reuse the same
movieScanContext variable (movieScanContext) across both calls and use
app.Queries for DB access, then assert the second call returns the same artist
ID and that the updated metadata (name/profilePath) was persisted (cached
handling didn't block saving). Locate the test block around getOrCreateArtist
subtests and add assertions verifying ID equality and updated persisted fields
to ensure the cache-hit path is covered.
