export type Int64Type = {
  Int64: number;
  Valid: boolean;
};

export type Float64Type = {
  Float64: number;
  Valid: boolean;
};

export type StringType = {
  String: string;
  Valid: boolean;
};
