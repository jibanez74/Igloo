// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: playlist_movies.sql

package database

import (
	"context"
	"database/sql"
)

const addMovieToPlaylist = `-- name: AddMovieToPlaylist :execrows
INSERT INTO playlist_movies (
  playlist_id,
  movie_id,
  position,
  added_by
)
VALUES
  (
    ?1,
    ?2,
    (
      SELECT COALESCE(MAX(position), -1) + 1
      FROM playlist_movies AS pm2
      WHERE pm2.playlist_id = ?1
    ),
    ?3
  )
ON CONFLICT (playlist_id, movie_id) DO NOTHING
`

type AddMovieToPlaylistParams struct {
	PlaylistID int64         `json:"playlist_id"`
	MovieID    int64         `json:"movie_id"`
	AddedBy    sql.NullInt64 `json:"added_by"`
}

// Zero rows affected means the movie was already in the playlist; the unique
// constraint replaces a separate membership pre-check.
func (q *Queries) AddMovieToPlaylist(ctx context.Context, arg AddMovieToPlaylistParams) (int64, error) {
	result, err := q.exec(ctx, q.addMovieToPlaylistStmt, addMovieToPlaylist, arg.PlaylistID, arg.MovieID, arg.AddedBy)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const countPlaylistMovies = `-- name: CountPlaylistMovies :one
SELECT
  COUNT(*)
FROM playlist_movies
WHERE playlist_id = ?
`

func (q *Queries) CountPlaylistMovies(ctx context.Context, playlistID int64) (int64, error) {
	row := q.queryRow(ctx, q.countPlaylistMoviesStmt, countPlaylistMovies, playlistID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getPlaylistMoviesPaginatedAsc = `-- name: GetPlaylistMoviesPaginatedAsc :many
SELECT
  m.id,
  m.title,
  m.poster_path,
  m.year,
  m.certification
FROM playlist_movies AS pm
INNER JOIN movies AS m
  ON m.id = pm.movie_id
WHERE pm.playlist_id = ?
ORDER BY
  LOWER(m.title) ASC,
  m.id ASC
LIMIT ?
OFFSET ?
`

type GetPlaylistMoviesPaginatedAscParams struct {
	PlaylistID int64 `json:"playlist_id"`
	Limit      int64 `json:"limit"`
	Offset     int64 `json:"offset"`
}

type GetPlaylistMoviesPaginatedAscRow struct {
	ID            int64          `json:"id"`
	Title         string         `json:"title"`
	PosterPath    sql.NullString `json:"poster_path"`
	Year          sql.NullInt64  `json:"year"`
	Certification sql.NullString `json:"certification"`
}

// Title order matches GET /api/movies/library sort=asc.
func (q *Queries) GetPlaylistMoviesPaginatedAsc(ctx context.Context, arg GetPlaylistMoviesPaginatedAscParams) ([]GetPlaylistMoviesPaginatedAscRow, error) {
	rows, err := q.query(ctx, q.getPlaylistMoviesPaginatedAscStmt, getPlaylistMoviesPaginatedAsc, arg.PlaylistID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetPlaylistMoviesPaginatedAscRow{}
	for rows.Next() {
		var i GetPlaylistMoviesPaginatedAscRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.PosterPath,
			&i.Year,
			&i.Certification,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getPlaylistMoviesPaginatedDesc = `-- name: GetPlaylistMoviesPaginatedDesc :many
SELECT
  m.id,
  m.title,
  m.poster_path,
  m.year,
  m.certification
FROM playlist_movies AS pm
INNER JOIN movies AS m
  ON m.id = pm.movie_id
WHERE pm.playlist_id = ?
ORDER BY
  LOWER(m.title) DESC,
  m.id DESC
LIMIT ?
OFFSET ?
`

type GetPlaylistMoviesPaginatedDescParams struct {
	PlaylistID int64 `json:"playlist_id"`
	Limit      int64 `json:"limit"`
	Offset     int64 `json:"offset"`
}

type GetPlaylistMoviesPaginatedDescRow struct {
	ID            int64          `json:"id"`
	Title         string         `json:"title"`
	PosterPath    sql.NullString `json:"poster_path"`
	Year          sql.NullInt64  `json:"year"`
	Certification sql.NullString `json:"certification"`
}

func (q *Queries) GetPlaylistMoviesPaginatedDesc(ctx context.Context, arg GetPlaylistMoviesPaginatedDescParams) ([]GetPlaylistMoviesPaginatedDescRow, error) {
	rows, err := q.query(ctx, q.getPlaylistMoviesPaginatedDescStmt, getPlaylistMoviesPaginatedDesc, arg.PlaylistID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetPlaylistMoviesPaginatedDescRow{}
	for rows.Next() {
		var i GetPlaylistMoviesPaginatedDescRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.PosterPath,
			&i.Year,
			&i.Certification,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const removeMovieFromPlaylist = `-- name: RemoveMovieFromPlaylist :exec
DELETE FROM playlist_movies
WHERE playlist_id = ?
  AND movie_id = ?
`

type RemoveMovieFromPlaylistParams struct {
	PlaylistID int64 `json:"playlist_id"`
	MovieID    int64 `json:"movie_id"`
}

func (q *Queries) RemoveMovieFromPlaylist(ctx context.Context, arg RemoveMovieFromPlaylistParams) error {
	_, err := q.exec(ctx, q.removeMovieFromPlaylistStmt, removeMovieFromPlaylist, arg.PlaylistID, arg.MovieID)
	return err
}
