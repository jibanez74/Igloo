# Igloo configuration
# Copy this file to .env next to the binary or in the repo root for development.

# INITIAL ADMIN ACCOUNT
# Used only on first launch when no admin user exists yet.
DEFAULT_ADMIN_NAME=Admin
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=ChangeMe123!

# NETWORK
# PORT controls the HTTP listener for both development and binary runs.
# SESSION_COOKIE_SECURE should be true behind HTTPS and false for plain HTTP.
PORT=8080
SESSION_COOKIE_SECURE=true

# STORAGE
# IGLOO_DATA_DIR is the default base directory for runtime files.
# Explicit DB_PATH, STATIC_DIR, LOGS_DIR, or TRANSCODE_DIR values override
# the paths derived from IGLOO_DATA_DIR.
IGLOO_DATA_DIR=./data
# DB_PATH=./data/igloo.db
# STATIC_DIR=./data/static
# LOGS_DIR=./data/logs
# TRANSCODE_DIR=./data/transcode

# MEDIA LIBRARY PATHS
# Set only the libraries you use. Each configured directory must already exist.
# MOVIES_DIR=/srv/media/movies
# SHOWS_DIR=/srv/media/shows
# MUSIC_DIR=/srv/media/music

# METADATA PROVIDERS
TMDB_API_KEY=
JELLYFIN_TOKEN=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# RUNTIME FEATURES
ENABLE_LOGGER=false
ENABLE_WATCHER=false
DOWNLOAD_IMAGES=false
LOG_TO_STDOUT=true

# TRANSCODING
# cpu is the portable default. apple is macOS VideoToolbox, intel is Linux QSV,
# and nvidia is Linux CUDA when the host has the required drivers and devices.
HARDWARE_ACCELERATION_DEVICE=cpu
# HLS_MAX_CPU_TRANSCODES=

# DEVELOPMENT
# IGLOO_ENV_FILE can point at a specific env file before startup.
# DEBUG enables debug logging. make dev also sets VITE_DEV_SERVER.
# IGLOO_ENV_FILE=/path/to/.env
# DEBUG=true
